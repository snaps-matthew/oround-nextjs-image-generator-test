import CATEGORY from 'apiResources/constants/CategoryCode'

export const getProductScale = (category:string) => {
  let scale = 1;
  // if(
  //   category === CATEGORY.FRAME ||
  //   category === CATEGORY.POLAROID_PACK ||
  //   category === CATEGORY.SEAL_STICKER
  // ){
  //   scale = 2;
  // }
  return scale
}
