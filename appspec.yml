version: 0.0
os: linux
files:
  - source: /oround-image-generator
    destination: /snaps/servers/oround-image-generator
permissions:
  - object: /snaps/servers/oround-image-generator
    owner: snapsadmin
    group: snapsadmin
    mode: 755
hooks:
  BeforeInstall:
    - location: /snaps/bin/before-install.sh
      timeout: 30
      runas: snapsadmin
